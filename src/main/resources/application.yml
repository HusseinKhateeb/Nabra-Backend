spring:
  application:
    name: nabra-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/nabra
    username: postgres
    password: MST

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

  jackson:
    time-zone: UTC

  mail:
    host: smtp.gmail.com
    port: 587
    username: odehteam935@gmail.com
    password: towdnaeenrkzeuhi
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080

app:
  security:
    jwt:
      secret: "change-me-change-me-change-me-change-me"
      expirationSeconds: 86400

  frontend:
    allowedOrigins:
      - "http://localhost:3000"
      - "http://localhost:3001"
      - "http://localhost:4200"

  ai:
    lipreading:
      baseUrl: ""
    prediction:
      baseUrl: ""
    stt:
      baseUrl: ""

logging:
  level:
    org.hibernate.SQL: debug
